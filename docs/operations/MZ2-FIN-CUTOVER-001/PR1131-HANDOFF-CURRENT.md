# MZ2-FIN-CUTOVER-001 — PR #1131 handoff

آخر تحديث: 2026-09-23

## الهوية والنطاق

- المستودع: `AMASI-SA/AMASI-SA`.
- المهمة: إكمال الصناديق والحسابات المالية والأرصدة الافتتاحية الموحدة على V2.
- الفرع: `chatgpt/mz2-ad-financial-accounts-opening-balances-20260922`.
- Draft PR: [#1131](https://github.com/AMASI-SA/AMASI-SA/pull/1131).
- قاعدة التنفيذ المعتمدة من #1130: `37a5ab60a8c42736c63be37457ae9e7019e82415`.
- أول رأس تنفيذ محلي مكتمل: `c032b230fa0c48c801ccf2d85d1bdbf619ad87fc`.
- رأس GitHub المكافئ له قبل إصلاح CI: `65faa965d6dc506d90226796b80e020e0749f3af`.
- بصمة شجرة التنفيذ المطابقة محليًا وعلى GitHub: `9d3a903d9395a374dd2a9ac8fe47f5cb1f8d1986`.
- مصدر إصلاح CI المحلي الموثق في هذا السجل: `c43bae90baaebe70db7df7d0d73eacfd7846923f`.

يبقى #1131 Draft ومفتوحًا وغير مدمج. لا يبدأ #1132 أو Preview Candidate من هذه النقطة دون مراجعة مستقلة وتفويض جديد.

## ما نُفذ في #1131

- صفحة مستقلة `financial-accounts` ومسارها:
  `/integrations-v2?workspace=financial&page=financial-accounts`.
- تعريف البنوك والصناديق والمحافظ والحسابات المالية دون إنشاء حركة أو رصيد عند التعريف.
- الأرصدة الافتتاحية الموحدة بمراحل منفصلة: Draft ثم Review ثم Post.
- الترحيل والعكس عبر الكاتب المرجعي الوحيد `accounting_ledger_v2`، بلا fallback إلى legacy.
- العكس إلحاقي ولا يعدل مجموعة القيد الأصلية.
- منع التكرار وإعادة المحاولة والتزامن داخل معاملات Mongo.
- صلاحيات مستقلة للعرض والإدارة والمسودات والمراجعة والترحيل والعكس.
- الصلاحيات الحساسة الجديدة لا تُمنح للمالك ضمنيًا؛ يجب منحها صراحة.
- المفتاح القديم `accounting.opening_balances.approve` لا يفتح مسار Draft/Review/Post الجديد.
- عقد دليل فعلي يستخدم `accounting_source_files.py::preserve_original`، مع فشل مغلق عند اختلاف المالك أو البصمة أو الحجم أو نسخة الاعتماد.
- حاجز انتقال الكاتب: `legacy_active -> transition_blocked -> v2_active`، مع منع الكاتبين معًا في الحالة الوسطى ومنع الرجوع التلقائي إلى legacy.
- حارس وصول يمنع استدعاء كتاب V2 الخام من مسارات غير معتمدة.

## الاختبارات قبل أول رفع

- Backend المتأثر: 62 ناجحًا.
- Real Mongo معزول بمعاملات: 14 ناجحًا.
- اختبارات منع الكتابة المزدوجة: 6 ناجحة.
- Frontend: 6 مجموعات و33 اختبارًا ناجحًا.
- بناء Frontend الكامل: ناجح.
- حارس وصول V2: ناجح.
- لم تستخدم Production أو Preview أو أي بيانات مالية حقيقية.

## الرفع الأول وحالة CI

رُفعت شجرة التنفيذ إلى GitHub دون force. أنشأ اتصال GitHub الموثق SHA مختلفًا عن المحلي مع الحفاظ على الشجرة نفسها حرفيًا.

على الرأس `65faa965d6dc506d90226796b80e020e0749f3af` نجحت الفحوص التالية، من ضمنها:

- Real-Mongo P01 regression.
- Accounting frontend contract.
- Mezan Release Readiness.
- Security Gate.
- CodeQL.
- Store Delivery وFulfillment وبقية البوابات الظاهرة.

فشل فحص واحد فقط:

- `MZ2 Accounting Module / Accounting backend contract`.
- السبب: اختبار قديم افترض ثماني صفحات بدل تسع، وافترض أن المالك يرث جميع المفاتيح الجديدة ضمنيًا.
- نتيجة CI قبل الإصلاح: 49 ناجحًا و2 فاشلان داخل ذلك الفحص.
- لم يكن الفشل في منطق التطبيق أو Real Mongo أو الواجهة.

## إصلاح CI

التعديل الوحيد بعد الرفع الأول هو:

- `backend/tests/test_mz2_accounting_module.py`.

صحح الاختبار ليثبت:

1. وجود الصفحة التاسعة `الصناديق والحسابات المالية`.
2. وجود مفاتيح manage/drafts/review/post الجديدة في العقد المركزي.
3. أن المالك لا يحصل على مفاتيح `ACCOUNTING_EXPLICIT_GRANT_KEYS` تلقائيًا.
4. أن منح هذه المفاتيح صراحة يعيد كتالوج الصلاحيات الكامل.

لم يتغير كود التطبيق في إصلاح CI.

## التحقق المحلي بعد إصلاح CI

- إعادة إنتاج الفشل أولًا: 2 فاشلان و8 ناجحة.
- بعد الإصلاح، `test_mz2_accounting_module.py`: 10 ناجحة.
- أمر CI الخلفي نفسه: 51 ناجحة.
- الحزمة الخلفية الموسعة: 113 ناجحة.
- `scripts/check_accounting_ledger_v2_access.py`: PASS.
- فشل وسيط واحد عند تشغيل الحزمة من `backend/` كان سببه مسار Python الذي أخفى حزمة `scripts`؛ أعيد التشغيل من جذر المستودع مع `PYTHONPATH=backend:.` ونجح 113/113. لم يُخف عيبًا في المنتج.

## الحالة والقيود

- `P01=IN_PROGRESS`.
- `P02=LOCKED`.
- `P03=LOCKED`.
- `Preview=UNCHANGED`.
- `Production=UNCHANGED`.
- `Production financial/config writes=NONE`.
- لا Merge أو Deploy أو Publish أو Release Guard أو UAT في هذه المرحلة.
- لا كتابات مالية حقيقية.

## نقطة الاستئناف الآمنة

1. تحقق أن فرع #1131 البعيد يحتوي إصلاح الاختبار وهذا السجل، وسجل SHA البعيد النهائي في Issue #1006.
2. انتظر اكتمال CI للرأس الجديد واقرأ كل الفحوص؛ لا تعتمد نجاح الرفع الأول بعد تغير الرأس.
3. إذا فشل فحص، أعد إنتاجه محليًا وحدد هل هو منتج أو اختبار أو بيئة قبل أي تعديل.
4. إذا نجحت كل الفحوص، أرسل تقرير مراجعة #1131 فقط. لا تبدأ #1132 ولا Preview Candidate أو UAT دون تفويض مستقل.

## استئناف 2026-09-23 — تقوية V2 قبل CI النهائي

استؤنف العمل من الرأس المحلي `d473d16ddc498235b526332f0f76deb5984f2229`، وكان رأس فرع GitHub قبل هذا الاستئناف `652d3ed1f37266c0d10e7356451d5b457252afa6`. كانت شجرتاهما متطابقتين رغم اختلاف معرفي الالتزام بسبب بوابة الرفع. رأس GitHub النهائي لهذا الاستئناف هو رأس فرع #1131 الذي يحتوي هذا المستند؛ ويجب تسجيل معرفه الصريح ونتيجة CI في Issue #1006 بعد الرفع.

### ما أُكمل في هذا الاستئناف

- أضيف قارئ تقارير V2 موثوق يتحقق من مجموعات القيود المنشورة ولا يجمع بين V2 وlegacy أو يرجع تلقائيًا إلى legacy عند فشل V2.
- أصبحت جاهزية الرصيد الافتتاحي تقبل دليل `source_file_id` المحفوظ فعليًا، مع التحقق من السلسلة الجذرية والاستبدال والعكس وتوقيت القطع.
- أُغلقت نافذة التزامن في تعديل وأرشفة الحساب المالي بإدخالهما ضمن `atomic_owner` نفسه.
- غُطيت حالات رفض أرقام `float` للأموال وسعر الصرف، واشتراط سعر صرف SAR = 1، ودليل سعر صرف العملة الأجنبية وتوقيته، وأقسام الأدلة الدقيقة وتوقيت `Asia/Riyadh`.
- قُوي حاجز الانتقال ليفشل مغلقًا عند الحالة الجزئية أو المراجعة غير المطابقة.
- أُضيفت تغطية Real Mongo تؤكد عزل تقارير V2 عن أي قيد legacy، وصحة أثر الاستبدال، وأن النشاط التشغيلي اللاحق لا يسد نقص الرصيد الافتتاحي.
- اكتمل مسار فتح صفر صريح ثم عكسه واستبداله بأول قيد افتتاحي V2 حقيقي مع حفظ سلسلة النسب وعدم تكرار القيد.
- صححت الواجهة اختيار هدف الاستبدال كي تستخدم أحدث افتتاحية منشورة ولا تعود إلى افتتاحية أقدم معكوسة.

### التحقق المحلي بعد التقوية

- عقد Backend الموسع: **87 اختبارًا ناجحًا**.
- اختبارات Backend المركزة: **36 ناجحًا**.
- اختبارات Real Mongo الجديدة: **15 اختبارًا موجودًا، لكنها SKIPPED محليًا** لعدم توفر `MZ2_TEST_MONGO_URI` أو Mongo replica set محلي. يجب أن يكون CI المزود بـMongo الحقيقي هو دليلها النهائي؛ لا تُعد هذه الاختبارات ناجحة محليًا.
- حارس وصول V2: **PASS**.
- Frontend: **23 مجموعة و105 اختبارات ناجحة**.
- بناء Frontend الكامل: **PASS** باستخدام Vite 8.2.1. التحذيرات عن ترتيب CSS وحجم الحزمة والاستيراد الديناميكي قديمة وغير مانعة.
- `git diff --check`: **PASS**.

جرّب تشغيل حزمة P01 Real Mongo محليًا، لكن الاختبارات توقفت قبل منطق المنتج لأن متغير `MZ2_TEST_MONGO_URI` غير موجود. هذا عائق بيئة محلية وليس نتيجة نجاح أو فشل للمنتج، ولا يجوز تمثيله كدليل Real Mongo.

### الخطوة الآمنة التالية

1. ارفع هذه الشجرة إلى فرع #1131 دون force وبعد مواءمتها مع رأس الفرع البعيد.
2. سجل رأس GitHub النهائي في Issue #1006.
3. انتظر CI، وبالأخص Real Mongo والمعاملات والتزامن والتراجع وعزل المالك والأدلة وحاجز الانتقال.
4. إذا نجح CI كله، يبقى القرار مراجعة #1131 فقط. لا تبدأ #1132 أو Preview أو UAT أو Production.

الحالة لم تتغير: `P01=IN_PROGRESS`، و`P02=LOCKED`، و`P03=LOCKED`، وPreview وProduction دون تغيير، والكتابات المالية الفعلية صفر.

## متابعة CI للرأس `c7bec61e` — Store Delivery

- نجحت البوابات المبكرة، لكن فحص `Store Delivery V1 / Store Delivery Backend` كشف تعارض اختبار قديم: كان سيناريو الترحيل المالي يستخدم `mongomock_motor` الذي لا يدعم أمر `hello` أو معاملات Mongo، بعد أن أصبح الكاتب المشترك يرفض أي قاعدة غير معاملية.
- لم يُضف fallback إلى التطبيق ولم يُفتح P02.
- حُوّل السيناريو المالي نفسه إلى قاعدة مؤقتة عشوائية على Mongo replica set معزول ينشئه Workflow، بينما بقيت اختبارات القفل غير المالية على Mongo الوهمي.
- التحقق المحلي لملف المحاسبة: **4 PASS و1 SKIPPED**؛ المتخطى هو السيناريو المالي فقط بسبب غياب `MZ2_TEST_MONGO_URI`، ولا يُحسب PASS.
- محاولة الحزمة الكاملة محليًا توقفت أثناء جمع ثلاثة ملفات جانبية لغياب `email-validator` في بيئة Python المحلية؛ الـWorkflow يثبت هذه الحزمة صراحة. يجب إثبات الإصلاح المالي وبقية الحزمة في CI المعزول.

## متابعة CI للرأس `dadf4d8c` — MZ2 Accounting

كشف CI ثلاثة عيوب اختبار/ترتيب تحقق، دون كتابة مالية جزئية:

1. اختبار حارس وصول V2 كان يعتمد على تشغيل pytest من جذر المستودع؛ أصبح يحمل السكربت من مساره المطلق المشتق من ملف الاختبار.
2. العبث بـ`approval_version` قبل تفعيل V2 يُرفض مبكرًا عند التفعيل نفسه؛ صُحح الاختبار ليثبت هذا الرفض وعدم إنشاء قيد بدل توقع تجاوز البوابة.
3. فحص مراجعة الانتقال أصبح يسبق فحوص جاهزية الافتتاحية داخل المعاملة؛ بذلك تعيد المراجعة القديمة `accounting_transition_revision_mismatch`، وأي فشل جاهزية لاحق يلغي انتقال الحالة كاملًا.

تحتاج هذه الإصلاحات دورة CI جديدة، ولا تُعد ناجحة حتى يجتاز Real Mongo والعقد الخلفي.
