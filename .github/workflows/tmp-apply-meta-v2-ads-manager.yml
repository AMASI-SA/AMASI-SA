name: Apply Meta V2 Ads Manager Source Fix

on:
  push:
    branches:
      - fix/ads-manager-meta-v2-source

permissions:
  contents: write

concurrency:
  group: apply-meta-v2-ads-manager-source
  cancel-in-progress: true

jobs:
  apply-test-push:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ads-manager-meta-v2-source
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply source selection and regression tests
        run: |
          python - <<'PY'
          from pathlib import Path

          service_path = Path('backend/ads_manager/service.py')
          test_path = Path('backend/tests/test_unified_ads_manager_phase1.py')
          service = service_path.read_text(encoding='utf-8')
          tests = test_path.read_text(encoding='utf-8')

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected one match, found {count}')
              return text.replace(old, new, 1)

          service = replace_once(
              service,
              'PROVIDER_ORDER = ("snapchat", "tiktok", "meta")\nPROVIDER_ALIASES = {',
              'PROVIDER_ORDER = ("snapchat", "tiktok", "meta")\n'
              'META_V2_PERFORMANCE_COLLECTION = "mezan_meta_performance_daily_v2"\n'
              'META_LEGACY_PERFORMANCE_COLLECTION = "meta_ads_daily"\n'
              'META_INTEGRATION_PROVIDER = "meta_ads"\n'
              'PROVIDER_ALIASES = {',
              'Meta collection constants',
          )

          old_source = '''    {
        "key": "meta_ads_daily",
        "role": "أداء حملات Meta المحفوظ محليًا",
        "grain": "حملة × يوم",
        "authoritative_for": [
            "meta_provider_reported_spend",
            "meta_campaign_identity",
            "meta_impressions",
            "meta_clicks",
            "meta_platform_attribution",
        ],
    },
'''
          new_source = '''    {
        "key": META_V2_PERFORMANCE_COLLECTION,
        "role": "أداء حسابات Meta الأصلي المحفوظ عبر Integrations V2",
        "grain": "حساب إعلاني محدد × يوم",
        "authoritative_for": [
            "meta_provider_reported_spend",
            "meta_impressions",
            "meta_clicks",
            "meta_platform_attribution",
        ],
    },
    {
        "key": META_LEGACY_PERFORMANCE_COLLECTION,
        "role": "مصدر Meta التاريخي الاحتياطي عند غياب مصدر V2",
        "grain": "حملة × يوم",
        "authoritative_for": [
            "meta_provider_reported_spend",
            "meta_campaign_identity",
            "meta_impressions",
            "meta_clicks",
            "meta_platform_attribution",
        ],
    },
'''
          service = replace_once(service, old_source, new_source, 'Meta source definitions')

          helper = '''def _normalize_meta_v2_rows(rows: list[dict]) -> list[dict]:
    """Adapt native Meta V2 account-day facts to the Ads Manager read model.

    Native V2 is account-grain, not campaign-grain.  The adapter preserves the
    provider-native currency and stored FX evidence, marks the row as aggregate
    only, and never mixes it with the historical ``meta_ads_daily`` source.
    """

    output: list[dict] = []
    for row in rows:
        account_id = _clean_text(row.get("ad_account_id"), limit=120)
        display_name = _clean_text(row.get("display_name"), limit=180)
        output.append(
            {
                "date": row.get("date"),
                "account_id": account_id or None,
                "campaign_id": "_default",
                "campaign_name": (
                    f"إجمالي {display_name}" if display_name else "إجمالي الحساب"
                ),
                "spend": row.get("spend_native"),
                "currency_native": row.get("currency_native"),
                "fx_rate": row.get("fx_rate_to_sar"),
                "purchases": row.get("purchases"),
                "purchase_value": row.get("purchase_value_native"),
                "impressions": row.get("impressions"),
                "clicks": row.get("clicks"),
                "updated_at": row.get("updated_at") or row.get("observed_at"),
                "_data_source": META_V2_PERFORMANCE_COLLECTION,
                "_aggregate_only": True,
            }
        )
    return output


'''
          anchor = 'def _campaign_rows(\n'
          if anchor not in service:
              raise SystemExit('campaign rows anchor missing')
          service = service.replace(anchor, helper + anchor, 1)

          old_data_source = '''                "data_source": (
                    "meta_ads_daily" if provider == "meta" else "tiktok_ads_daily"
                ),
'''
          new_data_source = '''                "data_source": (
                    _clean_text(row.get("_data_source"), limit=80)
                    or (
                        META_LEGACY_PERFORMANCE_COLLECTION
                        if provider == "meta"
                        else "tiktok_ads_daily"
                    )
                ),
'''
          service = replace_once(
              service,
              old_data_source,
              new_data_source,
              'campaign data source evidence',
          )

          old_meta_task = '''        meta_task = _rows(
            self.db,
            "meta_ads_daily",
            date_query,
            {
                "_id": 0,
                "date": 1,
                "account_id": 1,
                "campaign_id": 1,
                "campaign_name": 1,
                "spend": 1,
                "currency": 1,
                "currency_native": 1,
                "fx_rate": 1,
                "purchases": 1,
                "purchase_value": 1,
                "impressions": 1,
                "clicks": 1,
                "updated_at": 1,
            },
            limit=MAX_PERFORMANCE_ROWS,
            sort=[("date", 1), ("campaign_id", 1), ("account_id", 1)],
        )
'''
          new_meta_task = '''        meta_v2_task = _rows(
            self.db,
            META_V2_PERFORMANCE_COLLECTION,
            {**date_query, "provider": META_INTEGRATION_PROVIDER},
            {
                "_id": 0,
                "date": 1,
                "ad_account_id": 1,
                "display_name": 1,
                "spend_native": 1,
                "spend_sar": 1,
                "currency_native": 1,
                "fx_rate_to_sar": 1,
                "purchases": 1,
                "purchase_value_native": 1,
                "purchase_value_sar": 1,
                "impressions": 1,
                "clicks": 1,
                "empty_provider_row": 1,
                "observed_at": 1,
                "updated_at": 1,
            },
            limit=MAX_PERFORMANCE_ROWS,
            sort=[("date", 1), ("ad_account_id", 1)],
        )
        meta_legacy_task = _rows(
            self.db,
            META_LEGACY_PERFORMANCE_COLLECTION,
            date_query,
            {
                "_id": 0,
                "date": 1,
                "account_id": 1,
                "campaign_id": 1,
                "campaign_name": 1,
                "spend": 1,
                "currency": 1,
                "currency_native": 1,
                "fx_rate": 1,
                "purchases": 1,
                "purchase_value": 1,
                "impressions": 1,
                "clicks": 1,
                "updated_at": 1,
            },
            limit=MAX_PERFORMANCE_ROWS,
            sort=[("date", 1), ("campaign_id", 1), ("account_id", 1)],
        )
        meta_selected_accounts_task = _rows(
            self.db,
            "mezan_integration_accounts_v2",
            {
                "user_id": user_id,
                "provider": META_INTEGRATION_PROVIDER,
                "connection_provenance": "api_connection",
                "mezan_selected": True,
            },
            {
                "_id": 0,
                "external_account_id": 1,
                "ad_account_id": 1,
                "display_name": 1,
                "mezan_selected": 1,
            },
            limit=MAX_ACCOUNTS,
            sort=[("display_name", 1), ("external_account_id", 1)],
        )
'''
          service = replace_once(service, old_meta_task, new_meta_task, 'Meta read tasks')

          old_gather = '''            tiktok_rows,
            meta_rows,
            accounts,
            legacy_accounts,
            currency_settings,
        ) = await asyncio.gather(
            integration_task,
            booked_expense_task,
            snap_account_task,
            snap_accounts_task,
            snap_stats_task,
            tiktok_task,
            meta_task,
            accounts_task,
            legacy_accounts_task,
            currency_settings_task,
        )
'''
          new_gather = '''            tiktok_rows,
            meta_v2_rows,
            meta_legacy_rows,
            meta_selected_accounts,
            accounts,
            legacy_accounts,
            currency_settings,
        ) = await asyncio.gather(
            integration_task,
            booked_expense_task,
            snap_account_task,
            snap_accounts_task,
            snap_stats_task,
            tiktok_task,
            meta_v2_task,
            meta_legacy_task,
            meta_selected_accounts_task,
            accounts_task,
            legacy_accounts_task,
            currency_settings_task,
        )
'''
          service = replace_once(service, old_gather, new_gather, 'Meta gather tuple')

          old_limits = '''        source_limit_reached = {
            "snapchat_account_daily": len(snap_account_rows)
            > MAX_PERFORMANCE_ROWS,
            "snapchat_ad_accounts": len(snap_accounts) > MAX_ACCOUNTS,
            "snapchat_daily_stats": len(snap_stats_rows)
            > MAX_PERFORMANCE_ROWS,
            "tiktok_ads_daily": len(tiktok_rows) > MAX_PERFORMANCE_ROWS,
            "meta_ads_daily": len(meta_rows) > MAX_PERFORMANCE_ROWS,
            "ads_accounts": len(accounts) > MAX_ACCOUNTS,
            "counterparties": len(legacy_accounts) > MAX_ACCOUNTS,
        }
        snap_account_rows = snap_account_rows[:MAX_PERFORMANCE_ROWS]
        snap_accounts = snap_accounts[:MAX_ACCOUNTS]
        snap_stats_rows = snap_stats_rows[:MAX_PERFORMANCE_ROWS]
        tiktok_rows = tiktok_rows[:MAX_PERFORMANCE_ROWS]
        meta_rows = meta_rows[:MAX_PERFORMANCE_ROWS]
        accounts = accounts[:MAX_ACCOUNTS]
        legacy_accounts = legacy_accounts[:MAX_ACCOUNTS]
        dated_sources = {
            "snapchat_account_daily": snap_account_rows,
            "snapchat_daily_stats": snap_stats_rows,
            "tiktok_ads_daily": tiktok_rows,
            "meta_ads_daily": meta_rows,
        }
'''
          new_limits = '''        meta_v2_limit_reached = len(meta_v2_rows) > MAX_PERFORMANCE_ROWS
        meta_legacy_limit_reached = len(meta_legacy_rows) > MAX_PERFORMANCE_ROWS
        meta_selection_limit_reached = len(meta_selected_accounts) > MAX_ACCOUNTS
        snap_account_rows = snap_account_rows[:MAX_PERFORMANCE_ROWS]
        snap_accounts = snap_accounts[:MAX_ACCOUNTS]
        snap_stats_rows = snap_stats_rows[:MAX_PERFORMANCE_ROWS]
        tiktok_rows = tiktok_rows[:MAX_PERFORMANCE_ROWS]
        meta_v2_rows = meta_v2_rows[:MAX_PERFORMANCE_ROWS]
        meta_legacy_rows = meta_legacy_rows[:MAX_PERFORMANCE_ROWS]
        meta_selected_accounts = meta_selected_accounts[:MAX_ACCOUNTS]
        accounts = accounts[:MAX_ACCOUNTS]
        legacy_accounts = legacy_accounts[:MAX_ACCOUNTS]

        selected_meta_ids = {
            _clean_text(
                row.get("external_account_id") or row.get("ad_account_id"),
                limit=120,
            ).removeprefix("act_")
            for row in meta_selected_accounts
            if _clean_text(
                row.get("external_account_id") or row.get("ad_account_id"),
                limit=120,
            )
        }
        meta_v2_authoritative = bool(meta_v2_rows or meta_selected_accounts)
        if meta_v2_authoritative:
            meta_v2_rows = [
                row
                for row in meta_v2_rows
                if _clean_text(row.get("ad_account_id"), limit=120)
                .removeprefix("act_")
                in selected_meta_ids
            ]
            meta_rows = _normalize_meta_v2_rows(meta_v2_rows)
            meta_source_key = META_V2_PERFORMANCE_COLLECTION
            active_meta_limit_reached = (
                meta_v2_limit_reached or meta_selection_limit_reached
            )
        else:
            meta_rows = meta_legacy_rows
            meta_source_key = META_LEGACY_PERFORMANCE_COLLECTION
            active_meta_limit_reached = meta_legacy_limit_reached

        source_limit_reached = {
            "snapchat_account_daily": len(snap_account_rows)
            > MAX_PERFORMANCE_ROWS,
            "snapchat_ad_accounts": len(snap_accounts) > MAX_ACCOUNTS,
            "snapchat_daily_stats": len(snap_stats_rows)
            > MAX_PERFORMANCE_ROWS,
            "tiktok_ads_daily": len(tiktok_rows) > MAX_PERFORMANCE_ROWS,
            meta_source_key: active_meta_limit_reached,
            "ads_accounts": len(accounts) > MAX_ACCOUNTS,
            "counterparties": len(legacy_accounts) > MAX_ACCOUNTS,
        }
        dated_sources = {
            "snapchat_account_daily": snap_account_rows,
            "snapchat_daily_stats": snap_stats_rows,
            "tiktok_ads_daily": tiktok_rows,
            meta_source_key: meta_rows,
        }
'''
          service = replace_once(service, old_limits, new_limits, 'active Meta source selection')

          old_provider_source = '''            provider_source_key = (
                "snapchat_account_daily"
                if provider_key == "snapchat"
                else f"{provider_key}_ads_daily"
            )
'''
          new_provider_source = '''            provider_source_key = (
                "snapchat_account_daily"
                if provider_key == "snapchat"
                else meta_source_key
                if provider_key == "meta"
                else "tiktok_ads_daily"
            )
'''
          count = service.count(old_provider_source)
          if count != 2:
              raise SystemExit(f'provider source key: expected two matches, found {count}')
          service = service.replace(old_provider_source, new_provider_source)

          test_anchor = '''@pytest.mark.asyncio
async def test_provider_fact_and_booked_accounting_fact_stay_distinct():
'''
          new_tests = '''@pytest.mark.asyncio
async def test_meta_v2_is_authoritative_and_filters_to_selected_accounts():
    db = FakeDB(
        {
            "mezan_meta_performance_daily_v2": [
                {
                    "user_id": OWNER_ID,
                    "provider": "meta_ads",
                    "ad_account_id": "act-selected",
                    "display_name": "Selected Meta",
                    "date": "2026-07-10",
                    "spend_native": 10,
                    "spend_sar": 37.5,
                    "currency_native": "USD",
                    "fx_rate_to_sar": 3.75,
                    "purchases": 2,
                    "purchase_value_native": 20,
                    "purchase_value_sar": 75,
                    "impressions": 1000,
                    "clicks": 50,
                    "updated_at": "2026-07-28T10:00:00+00:00",
                },
                {
                    "user_id": OWNER_ID,
                    "provider": "meta_ads",
                    "ad_account_id": "act-not-selected",
                    "display_name": "Old Meta",
                    "date": "2026-07-10",
                    "spend_native": 1000,
                    "spend_sar": 3750,
                    "currency_native": "USD",
                    "fx_rate_to_sar": 3.75,
                    "purchases": 100,
                    "purchase_value_native": 2000,
                    "purchase_value_sar": 7500,
                    "impressions": 100000,
                    "clicks": 5000,
                    "updated_at": "2026-07-28T10:00:00+00:00",
                },
                {
                    "user_id": OTHER_OWNER_ID,
                    "provider": "meta_ads",
                    "ad_account_id": "act-other-owner",
                    "date": "2026-07-10",
                    "spend_native": 999,
                    "currency_native": "SAR",
                    "fx_rate_to_sar": 1,
                    "purchases": 99,
                    "purchase_value_native": 999,
                    "impressions": 9999,
                    "clicks": 999,
                },
            ],
            "mezan_integration_accounts_v2": [
                {
                    "user_id": OWNER_ID,
                    "provider": "meta_ads",
                    "connection_provenance": "api_connection",
                    "external_account_id": "act-selected",
                    "display_name": "Selected Meta",
                    "mezan_selected": True,
                },
                {
                    "user_id": OWNER_ID,
                    "provider": "meta_ads",
                    "connection_provenance": "api_connection",
                    "external_account_id": "act-not-selected",
                    "display_name": "Old Meta",
                    "mezan_selected": False,
                },
            ],
            "meta_ads_daily": [
                {
                    "user_id": OWNER_ID,
                    "date": "2026-07-10",
                    "account_id": "act-legacy",
                    "campaign_id": "legacy-campaign",
                    "campaign_name": "Legacy Meta",
                    "spend": 999,
                    "currency": "SAR",
                    "purchase_value": 999,
                    "purchases": 99,
                    "impressions": 9999,
                    "clicks": 999,
                }
            ],
        }
    )

    result = await _service(db).overview(
        OWNER_ID,
        date_from="2026-07-10",
        date_to="2026-07-10",
        provider="meta",
    )

    provider = result["providers"][0]
    assert provider["metrics"]["provider_reported_spend_sar"] == 37.5
    assert provider["metrics"]["platform_attributed_revenue_sar"] == 75
    assert provider["metrics"]["platform_reported_purchases"] == 2
    assert provider["campaign_coverage"]["status"] == "aggregate_only"
    assert result["campaign_pagination"]["total"] == 1
    assert result["campaigns"][0]["campaign_id"] == "_default"
    assert result["campaigns"][0]["data_source"] == (
        "mezan_meta_performance_daily_v2"
    )
    serialized = json.dumps(result, ensure_ascii=False)
    assert "legacy-campaign" not in serialized
    assert "act-not-selected" not in serialized
    assert OTHER_OWNER_ID not in serialized
    assert db.write_attempts == []


@pytest.mark.asyncio
async def test_meta_v2_selection_without_rows_does_not_fall_back_to_legacy():
    db = FakeDB(
        {
            "mezan_integration_accounts_v2": [
                {
                    "user_id": OWNER_ID,
                    "provider": "meta_ads",
                    "connection_provenance": "api_connection",
                    "external_account_id": "act-selected",
                    "display_name": "Selected Meta",
                    "mezan_selected": True,
                }
            ],
            "meta_ads_daily": [
                {
                    "user_id": OWNER_ID,
                    "date": "2026-07-10",
                    "account_id": "act-legacy",
                    "campaign_id": "legacy-campaign",
                    "campaign_name": "Legacy Meta",
                    "spend": 50,
                    "currency": "SAR",
                    "purchase_value": 100,
                    "purchases": 2,
                    "impressions": 500,
                    "clicks": 20,
                }
            ],
        }
    )

    result = await _service(db).overview(
        OWNER_ID,
        date_from="2026-07-10",
        date_to="2026-07-10",
        provider="meta",
    )

    assert result["metrics"]["provider_reported_spend_sar"] is None
    assert result["campaigns"] == []
    assert result["providers"][0]["campaign_coverage"]["status"] == (
        "unavailable"
    )
    assert db.write_attempts == []


'''
          if test_anchor not in tests:
              raise SystemExit('test insertion anchor missing')
          tests = tests.replace(test_anchor, new_tests + test_anchor, 1)

          service_path.write_text(service, encoding='utf-8')
          test_path.write_text(tests, encoding='utf-8')
          PY

      - name: Compile changed backend files
        run: |
          python -m py_compile \
            backend/ads_manager/service.py \
            backend/tests/test_unified_ads_manager_phase1.py

      - name: Install targeted test dependencies
        run: |
          python -m pip install --disable-pip-version-check -q \
            pytest pytest-asyncio fastapi httpx pydantic

      - name: Run Ads Manager safety suite
        env:
          PYTHONPATH: backend
          MEZAN_ADS_MANAGER_READ_ONLY_ENABLED: 'true'
        run: pytest -q backend/tests/test_unified_ads_manager_phase1.py

      - name: Verify no provider or accounting write surface was added
        run: |
          python - <<'PY'
          import ast
          from pathlib import Path

          tree = ast.parse(Path('backend/ads_manager/service.py').read_text())
          forbidden = {
              'insert_one', 'insert_many', 'update_one', 'update_many',
              'replace_one', 'delete_one', 'delete_many', 'bulk_write',
              'post', 'put', 'patch', 'delete',
          }
          found = []
          for node in ast.walk(tree):
              if isinstance(node, ast.Attribute) and node.attr in forbidden:
                  found.append(node.attr)
          if found:
              raise SystemExit(f'Forbidden operations in Ads Manager service: {sorted(set(found))}')
          print('ADS_MANAGER_META_V2_READ_ONLY_GUARD_OK')
          PY

      - name: Commit verified implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend/ads_manager/service.py \
                  backend/tests/test_unified_ads_manager_phase1.py
          git diff --cached --check
          git commit -m 'fix(ads): read authoritative Meta V2 performance source'
          git push origin HEAD:fix/ads-manager-meta-v2-source
