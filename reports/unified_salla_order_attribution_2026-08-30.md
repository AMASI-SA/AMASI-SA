# تقرير مطابقة طلبات سلة وإسنادها — 30 أغسطس 2026

## النطاق

- مصدر حقيقة طلبات ومبيعات المتجر: Salla API عبر إضافة «إدارة أماسي».
- مصدر العرض التشغيلي: Production `https://mezansalla.com/orders-v2` عبر المتصفح السحابي.
- مسار وأرقام Snapchat لم تُعدّل. لا يوجد شرط مساواة بين مشتريات Snapchat وطلبات سلة.
- `baseline_cutoff_at`: `2026-08-30T17:59:52+03:00` (`2026-08-30T10:59:52-04:00` في `America/New_York`).
- لا يتضمن التقرير أي بيانات عميل حساسة.

## المطابقة الحية قبل الإصلاح

| Salla reference | created_at الأصلي (Asia/Riyadh) | المبلغ في سلة | المبلغ في ميزان | المصدر في ميزان |
|---|---:|---:|---:|---|
| 281358491 | 2026-08-30 16:22:43 | 186.92 SAR | 186.92 SAR | Snapchat |
| 281358507 | 2026-08-30 16:22:47 | 186.92 SAR | 186.92 SAR | Snapchat |
| 281367168 | 2026-08-30 16:57:44 | 189.13 SAR | 189.13 SAR | Snapchat |

النتيجة: الطلبات الثلاثة موجودة مرة واحدة في المصدرين، وقيمة خط الأساس `562.97 SAR`.
كل الطلبات مدفوعة في سلة وليست بانتظار الدفع. تقع جميعها في يوم 30 أغسطس 2026 في `America/New_York`.

## الخلل قبل الإصلاح

- صفحة الطلبات تقبل رقم طلب دقيقًا وحالة فقط.
- تفاصيل الطلب `281367168` تعرض `campaign_id` كقيمة الحملة بدل الاسم.
- UTM الخام، Ad Squad/Ad، طريقة المطابقة، سبب عدم الاكتمال وتوقيت الحساب غير ظاهرة.
- لا يوجد ملخص مالي آمن لنتيجة مجموعة فلاتر.

## بعد الإصلاح في الفرع

- عقد Order Engine يحفظ UTM الخام كما وصل، مع نسخة normalized منفصلة.
- يدعم Campaign/Ad Squad/Ad IDs وأسماءها، Click IDs، حالة/طريقة/ثقة المطابقة وسبب عدم الإسناد.
- البحث يقبل عدة فلاتر مجتمعة ويستخدم `created_at` الأصلي للفترة.
- الملخص يستبعد انتظار الدفع والإلغاء والاسترجاع من المبيعات المدفوعة.
- إعادة Webhook/Backfill لا تضخم القراءة: رقم الطلب هو مفتاح منع التكرار.
- تفاصيل الطلب تعرض قسم «الإسناد الإعلاني» وتحذير Fail-Closed عند النقص أو التعارض.
- أسماء كيانات Snapchat تُقرأ من كتالوج الكيانات المحلي في ميزان؛ لا يُعاد احتساب أرقام Snapchat ولا تعديلها.

## عقد المصدرين المستقلين

- مشتريات Snapchat تُقارن بحساب Snapchat فقط.
- طلبات ومبيعات سلة تُقارن بسلة فقط.
- الفرق بينهما فرق إسناد معروض، وليس خطأ يستوجب تغيير أحد المصدرين.
- صفر مشتريات Snapchat مع ثلاثة طلبات سلة مطابقة بـ `campaign_id` حالة صحيحة ممكنة.

## التحقق الآلي

- Fixture ثابت للطلبات الثلاثة منفصل عن Production الحي.
- 5 اختبارات إسناد مستهدفة نجحت: خط الأساس، المبلغ، المطابقة بـ `campaign_id`، UTM الخام، منع التكرار، `created_at`، New York، والاستبعاد المالي.
- فحص `py_compile` و`git diff --check` نجح.
- لم يحدث دمج أو نشر أو تعديل حملة/ميزانية/Bid/Target Cost.
