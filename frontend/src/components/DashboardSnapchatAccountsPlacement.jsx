/*
 * Retired with the legacy dashboard UI.
 *
 * The supported advanced dashboard owns the advertising summary. This
 * compatibility component intentionally mounts no portals, observers, or
 * additional Snapchat polling requests.
 */
export default function DashboardSnapchatAccountsPlacement() {
    return null;
}
